# 2026-09-11

## 建立工作区记忆（从 .mnemon 注入）
- 读取 `E:\Modelica Code\.mnemon\`：`runtime/MEMORY.md`（12 条，与 `memories.json` 一致）、`runtime/USER.md`、`documents/index.json` + `documents/active/*.md`（30 篇，覆盖 2026-08-21 ~ 09-08）、`data/.dsh-memory-bodies.json`（body：Modelica建模）。
- 全量蒸馏后落地：
  - `.workbuddy/memory/MEMORY.md` —— 长期项目笔记：硬性纪律、设计基准、正常值锚点、工作流约定、长期未决（自动载入层）。
  - `.workbuddy/memory/mnemon-project-context.md` —— 详细技术档案：模型库地图、论文数据基线、组件实现要点、标定参数终值、**故障—根因—修复总表（11 类）**、无 PID 自稳与 flowDown 结论、斯特林图件资产、待办清单。
- 约定：`.mnemon` 保留为原始记忆库，`.workbuddy/memory` 为其蒸馏层；后续会话先读 MEMORY.md，需要细节再查 project-context.md，仍不足才回查 `.mnemon/documents/active/` 原文。

## 本次记忆的关键取舍
- 「禁用 TY 商业组件」「禁用 reload_mo_path」「结果只用 OpenResult+GetVarValueAt」「Layout 照源模型原样」等属用户明确定下的纪律，放在 MEMORY.md 最高优先级。
- 历史误诊（LimPID chattering / 飞轮 inertia 初始化）与其修正结论一并保留，避免重复踩坑。
- 蒸馏时核对：PrimaryLoop 与 flowDown 曾因堆芯换热关联式不一致（core 棒束 vs 默认 circle 圆管）导致额定壁温差 27 K，已由用户于 09-08 同步为 core，非遗留缺陷。
